import { mount } from 'enzyme';
import { ManageCoursePage, emptyCourseGeneraor } from './ManageCoursePage';
import expect from 'expect';

export const manageCoursePageTest = describe("ManageCoursePage Tests", () => {
    // it("Set error Message when title is empty", () => {

    //     const authors: any = [];
    //     const actions: any = { save: () => { return Promise.resolve(); } };

    //     const wrapper = mount(<ManageCoursePage authors={authors} courseNotFount={false} loading={false} course={emptyCourseGeneraor()} actions={actions} />);
    //     const saveButton = wrapper.find('input').last();

    //     expect(saveButton.props().type).toBe('submit');
    //     saveButton.simulate('click');

    //     expect(wrapper.state().errors.title).toBe('Title must be at least 5 characters.');
    // })
})